{
  "name": "example-llm",
  "version": "1.0.0",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "build": "bls-sdk-ts build ./index.ts",
    "build:mcp": "bls-sdk-ts build ./mcp.ts",
    "invoke": "bls-runtime ./build/index.wasm",
    "invoke:mcp": "bls-runtime ./build/mcp.wasm"
  },
  "devDependencies": {
    "@blockless/sdk-ts": "file:../.."
  }
}
